---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - buildah-create-manifest.yaml

patches:
  - target:
      name: buildah-create-manifest
      kind: Task
    patch: |-
      - op: replace
        path: "/kind"
        value: "ClusterTask"
